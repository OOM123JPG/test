# 1. 清理 CANN 算子编译缓存 (这是解决“没反应”最有效的手段)
rm -rf /tmp/operator_compile_cache/*
rm -rf ~/.cache/atb/*

# 2. 清理 Python 编译文件
cd /nfs-share/wx1463835/tdmoe/
find . -name "*.pyc" -delete
find . -name "__pycache__" -type d -exec rm -rf {} +

# 3. 清理模型目录下的缓存
cd /nfs-share/wx1463835/download/model/Deepseek-V3-bf16/
find . -name "__pycache__" -type d -exec rm -rf {} +
